-- ══════════════════════════════════════════════════════════
-- 1) AYUSIN ANG MEMO UPLOAD  (kulang na column)
-- 2) ILAGAY ANG ACCOUNTING RACI mula sa Raci Matrix.xlsx
-- Kopyahin lahat, i-paste sa Supabase SQL Editor, pindutin ang Run.
-- ══════════════════════════════════════════════════════════

-- ── 1. Memo upload fix ────────────────────────────────────
ALTER TABLE admin_memos ADD COLUMN IF NOT EXISTS storage_path text;
NOTIFY pgrst, 'reload schema';


-- ── 2. RACI table (kung hindi pa nagagawa) ────────────────
CREATE TABLE IF NOT EXISTS raci_matrix (
  id          bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  process     text NOT NULL,
  dept        text NOT NULL,
  description text,
  responsible text NOT NULL,
  accountable text NOT NULL,
  consulted   text,
  informed    text,
  created_at  timestamptz DEFAULT now() NOT NULL
);
CREATE INDEX IF NOT EXISTS raci_matrix_dept_idx ON raci_matrix (dept);
ALTER TABLE raci_matrix DISABLE ROW LEVEL SECURITY;
GRANT ALL ON raci_matrix TO anon;
GRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA public TO anon;


-- ── 3. Linisin ang lumang accounting rows, ilagay ang totoo ──
DELETE FROM raci_matrix WHERE dept = 'accounting';

INSERT INTO raci_matrix (process, dept, description, responsible, accountable, consulted, informed) VALUES

('Accounts Payable (AP)', 'accounting',
 'Pagproseso ng supplier invoices at pagbabayad.',
 'AP TL',
 'AAM',
 'GLA',
 'Accounting Manager, AR TL, Payroll'),

('Accounts Receivable (AR)', 'accounting',
 'Billing, collection, at pag-monitor ng customer accounts.',
 'AR TL',
 'AAM',
 'GLA',
 'Accounting Manager, AP TL, Payroll'),

('General Ledger (GL)', 'accounting',
 'Journal entries at pagpapanatili ng general ledger.',
 'AAM, GLA',
 'Accounting Manager, AAM',
 'Payroll',
 'AP TL, AR TL'),

('Tax', 'accounting',
 'Paghahanda at pagsusumite ng tax returns at compliance.',
 'AAM, GLA',
 'Accounting Manager, AAM',
 'AP TL, AR TL, Payroll',
 NULL),

('Payroll', 'accounting',
 'Pagkalkula at pagproseso ng sahod ng empleyado.',
 'AAM, Payroll',
 'Accounting Manager, AAM',
 'GLA',
 'AP TL, AR TL'),

('Closing', 'accounting',
 'Month-end at year-end closing ng mga libro.',
 'AAM, GLA',
 'Accounting Manager',
 'AP TL, AR TL, Payroll',
 NULL),

('Financial Reporting', 'accounting',
 'Paghahanda ng financial statements at management reports.',
 'AAM, GLA',
 'Accounting Manager',
 'AP TL, AR TL, Payroll',
 NULL);


-- ── 4. Tingnan ang resulta ────────────────────────────────
SELECT process, responsible, accountable, consulted, informed
FROM raci_matrix WHERE dept = 'accounting' ORDER BY id;
